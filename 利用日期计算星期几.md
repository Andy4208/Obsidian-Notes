## 问题：

给定某天的日期 $y, m, d$(年月日)，求出该天是周几.

## 算法一

### 蔡勒公式(Zeller's Congruence)

$$\begin{array}{l}
    D = \left[\dfrac{c}{4}\right] - 2c + y + \left[\dfrac{y}{4}\right] + \left[\dfrac{13(m + 1)}{5}\right] + d - 1 \\
    W = D \mod 7
\end{array}$$

其中：
- $c$ 为年份前两位
- $y$ 为年份后两位
- $m$ 为月份，$1$、$2$ 月看作前一年的 $13$、$14$ 月
- $d$ 为天
- $W$ 为星期几

代码中的公式是经过简化的。

代码：
```
int dow(int y, int m, int d) {
    if (m < 3) y--, m += 12;
    return (y + y / 4 - y / 100 + y / 400 + 13 * (m + 1) / 5 + d - 1) % 7;
}
```

## 算法二

### 坂本智彦算法(Tomohiko Sakamoto算法)

更好理解了，其中的数组是每个月长度的前缀和对 $7$ 取模的结果，字符串则是找了一些与之同余的字符.

代码
```
int dow(int y, int m, int d) {
    static int t[] = {0, 3, 2, 5, 0, 3, 5, 1, 4, 6, 2, 4};
    y -= m < 3;
    return (y + y / 4 - y / 100 + y / 400 + t[m - 1] + d) % 7;
}
```

精简版代码
```
int dow(int y, int m, int d) {
    y -= m < 3;
    return (y + y / 4 - y / 100 + y / 400 + "-bed=pen+mad."[m] + d) % 7;
}
```

### 


## 参考

[洛谷 蔡勒公式](https://www.luogu.com.cn/article/47y99101)

[知乎 Tomohiko Sakamoto算法](https://www.zhihu.com/question/358255792/answer/2945700771)