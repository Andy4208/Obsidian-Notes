
让我们看一下这段代码：

``` C
float Q_rsqrt(float number) {
    long i;
    float x2, y;
    const float threehalfs = 1.5F;

    x2 = number * 0.5F;
    y = number;
    i = *(long *)&y;           // evil floating point bit level hacking
    i = 0x5f3759df - (i >> 1); // what the fuck?
    y = *(float *)&i;
    y = y * (threehalfs - (x2 * y * y)); // 1st iteration
    //  y  = y * ( threehalfs - ( x2 * y * y ) );   // 2nd iteration, this can be removed

    return y;
}
```

这是一段来自于 1999 年发售的游戏《雷神之锤 3：竞技场》中的代码. 其用途是计算平方根倒数，即 $f(x) = \dfrac{1}{\sqrt{ x }}$. 在当时的年代，计算机性能比较落后，编译器的优化也远远不如今日，一般的 ``sqrt`` 函数和除法的速度满足不了游戏 $30$ 帧正常运行的需求. 因此作者把 C 语言的位运算运用到极致，写出了以上这段代码，主要用于 3D 图形学中的向量单位化，即计算 $\dfrac{\mathrm{x}}{|\mathrm{x}|}$.

下面讲解这段代码的原理. 首先，根据``IEEE 754``标准，``float``类型为 $32$ 为移码格式的浮点数，其最高位为符号位，紧跟着 $8$ 位阶数码 $e$，然后是 $23$ 位尾数码 $m$，(正数) 表示为

$$
    x = (1.m \times 10^{e - 1111111})_{2} = \left( 1 + \frac{m}{2^{23}} \right) \times 2^{e-127}
$$

例如：
$$
\begin{align*}
(0\hspace{0.5em}10000010\hspace{0.5em}00101100000000000000000)_{\text{float}}
& = (1.001011 \times 10 ^ {10000010 - 1111111})_{2} \\
& = \left( 1 + \frac{1441792}{2^{23}} \right) \times 2^{130 - 127} \\
& = \left( 1 + \frac{11}{64} \right) \times 2 ^ {3} = 9.375

\end{align*}
$$

因此，这段代码中的``i = *(long *)&y``表示将浮点型``y``的二进制码复制给整型``i``，于是我们有 $i = m + e \times 2^{23}$.

我们把这样强制转整型的操作记为 $y'$. 注意到，
$$
\begin{align*}
\log_{2}y & = \log_{2}\left( 1+\frac{m}{2^{23}} \right) + e - 127 \\
& \approx \frac{m}{2^{23}} + \alpha + e - 127 \\
& = \frac{y'}{2^{23}} + \alpha - 127
\end{align*}
$$

也就是说，其实上面的强制转整型的操作实际上近似于取了个对数！令 $z=\dfrac{1}{\sqrt{y}}$，进而我们有，
$$
\begin{align*}
\frac{z'}{2^{23}} + \alpha - 127 & = \log_{2}z\\
& = -\frac{1}{2}\log_{2}y\\
& = -\frac{1}{2}\left(\frac{y'}{2^{23}} + \alpha - 127 \right)
\end{align*}
$$

可以算出 $z'=3\times 2^{22}(127-\alpha)-\dfrac{y'}{2}$. 

上述 $\alpha$ 取在 $0.045$ 左右时较优，上述代码中的 $\alpha$ 取值约为 $0.0450466$，可能是作者多次实验选取的较优解(个人认为网上那些反推魔数来源的帖子不太靠谱，因为一个程序员选取魔数的最好方式显然是遍历附近的至多 $10^5$ 个数，再取几个数计算误差).

此时算出的 $z$ 实际上只是 $\dfrac{1}{\sqrt{ x }}$ 的近似值，因此作者选择了使用牛顿迭代进行进一步逼近. 具体地，取 $f(y)=\dfrac{1}{y^2}-x$，我们有牛顿迭代公式 $y_{n+1}=y_{n}-\dfrac{f(y_{n})}{f'(y_{n})}=y_{n} \dfrac{3-xy_{n}^2}{2}$.

## 参考：

[计算平方根倒数的神奇代码](https://www.bilibili.com/video/BV13xN96XE9g/?spm_id_from=333.337.search-card.all.click&vd_source=87cb0541bdd5704fb2a4dc0d45f77d71)